import type { RichSubnet } from '../subnet-rich';

export const sn108: RichSubnet = {
  slug: '108-talkhead',
  netuid: 108,
  name: 'TalkHead',
  shortPitch: 'Lip-synced photorealistic talking-head video generation — image + text → video.',
  overview: [
    'TalkHead is Bittensor Subnet 108, a decentralized photorealistic talking-head subnet. Miners take a reference image, a piece of text, and an optional voice profile and render a lip-synced video clip of that face speaking that text. Validators score realism, identity preservation, and motion quality, applying a latency bonus on top of quality before setting on-chain weights.',
    'The work loop is explicit in the published codebase. Validators query miners with a "TalkHead Synapse" carrying image_base64, text, and voice_profile. Miners forward the synapse to their own talking-head video generation API, render the clip, upload it to Cloudflare R2, and return a public video URL. Validators then send the URL to a scoring server that computes quality metrics — sync, identity, motion — across the clip.',
    'TalkHead positions itself as a distributed evaluation platform for talking-head AI models: anyone can submit a containerised model, get ranked on the same metrics, and earn rewards for top scores. This sets it up to be both a benchmark for the talking-head research community and a product API for downstream apps that need on-demand lip-synced video.',
    'One-line diff: a continuously-evaluated, TAO-paid leaderboard for talking-head video generation. <a href="#customer" style="color: var(--accent);">See competitive landscape ↓</a>',
  ],
  cycle: {
    challenge: { actor: 'Validator', title: 'Send synapse', body: 'Validator builds a TalkHead Synapse carrying image_base64, text, and optional voice_profile, then dispatches it to a set of registered miners.', dataK: 'payload', dataV: 'image + text + voice' },
    compute:   { actor: 'Miner',     title: 'Render clip', body: 'Miner forwards the synapse to their own talking-head model API, renders a lip-synced clip, uploads it to Cloudflare R2, and returns the public video URL to the validator.', dataK: 'latency',  dataV: 'time-to-clip' },
    score:     { actor: 'Validator', title: 'Score realism', body: 'Validator sends the URL to a scoring server that computes sync, identity-preservation, and motion-quality metrics over the clip; a latency bonus is applied on top of quality before weights are set.', dataK: 'scale',    dataV: 'quality × latency-bonus' },
    settle:    { actor: 'Subtensor', title: 'Yuma → emission', body: "Every validator's weights, multiplied by their stake, aggregated by Yuma into one reward vector. TAO mints to top miners.", dataK: 'tempo', dataV: '~72 min · 24×/day' },
  },
  miner:     { does: 'Run a talking-head video generation model behind an API, accept TalkHead Synapses, render lip-synced clips, upload to Cloudflare R2, and return URLs.', input: 'TalkHead Synapse: image_base64 + text + optional voice_profile', output: 'Public Cloudflare R2 URL pointing to the rendered video clip', hardware: 'GPU node sized for talking-head video generation (model-dependent — typically a single workstation-class GPU)', paidFor: 'High realism / identity preservation / motion quality and low latency', paidVia: 'Per-tempo emission, score × validator stake' },
  validator: { does: 'Generate TalkHead Synapses, send them to miners, hand returned video URLs to the scoring server, and set on-chain weights from realism + latency scores.', requires: 'Validator stack with TalkHead Synapse generator + scoring-server integration, registered Bittensor validator hotkey', output: 'Weight vector across talking-head miners', paidFor: 'Submitting weights that agree with consensus median', paidVia: 'Per-tempo emission, stake × consensus alignment' },
  scoring:   { leadOneLine: 'Quality first (sync + identity + motion), latency bonus on top — not raw latency.', explanation: ['The scoring server runs three classes of metrics per returned clip: lip-sync alignment against the spoken text/voice profile, identity preservation against the reference image, and motion quality (no flicker, no morph). These run on the clip itself, not on miner self-reports.', 'A latency bonus is applied multiplicatively after quality scoring, so fast-but-bad and slow-but-perfect both lose to fast-and-good. This is the right shape for a production API benchmark: realism is the hard floor, latency is what wins the tiebreak.'], cheatPath: 'Pre-rendered cache attacks (reuse a clip that happened to match) are defeated by validators randomising image/text/voice in each synapse. Submitting a clip that\'s identity-correct but doesn\'t actually lip-sync is defeated by the sync metric. The subtler attack is gaming a single quality dimension — but the multi-metric score plus latency bonus pushes against narrow over-optimisation.' },
  customer:  { leadOneLine: 'Any app or agent that needs on-demand lip-synced talking-head video and wants a non-API-locked alternative to closed providers.', explanation: ['The clearest customers are downstream products: AI avatars for customer support, marketing video tools, education platforms generating lecturer clips, and agent frameworks that need a visual front-end. These users today pay HeyGen, D-ID, or Synthesia for the same primitive — a face + text → video API call.', 'TalkHead\'s pitch is an open, continuously-benchmarked alternative where the best model on the leaderboard is always live and price competition is structural, not a closed-vendor courtesy.'] },
  competitive: { scope: 'talking-head video generation · 2026', rows: [
    { name: 'TalkHead', subtitle: 'SN108', isSelf: true, approach: 'Open distributed evaluation + generation: miners run models, validators score realism, TAO pays for winners.', access: 'open · API', accessTone: 'open', differentiator: 'Only Bittensor-native talking-head leaderboard with on-chain weighting and TAO emissions.' },
    { name: 'HeyGen', approach: 'Centralised AI avatar / talking-head SaaS with strong product UX and stock-avatar library.', access: 'closed · SaaS', accessTone: 'closed', differentiator: 'Largest commercial player; polished but closed-source and per-seat priced.' },
    { name: 'D-ID', approach: 'Talking-head / animated-avatar API with broad enterprise integrations.', access: 'closed · API', accessTone: 'closed', differentiator: 'API-first commercial competitor; closed weights, vendor lock-in.' },
    { name: 'Synthesia', approach: 'Enterprise AI video platform built around branded talking-head avatars.', access: 'closed · SaaS', accessTone: 'closed', differentiator: 'Enterprise sales motion and polished avatars but most expensive and least flexible.' },
    { name: 'Open-source models (SadTalker, EMO, V-Express)', approach: 'Public research models for talking-head generation, self-hosted by users.', access: 'open · self-host', accessTone: 'open', differentiator: 'Free if you operate it yourself, but no managed API and no continuous benchmarking.' },
  ], note: 'Talking-head generation is one of the cleanest "model behind an API" markets in AI — three closed-source SaaS providers (HeyGen, D-ID, Synthesia) currently dominate. TalkHead\'s thesis is that this primitive is commoditisable: pay any GPU operator to run the best open model, score them objectively, and let the cheapest accurate provider win.' },
  team: { intro: ['TalkHead is built by the TalkHead AI team — public on GitHub as talkheadai/talkhead-subnet, with the repo explicitly described as a "Decentralized photorealistic talking head subnet on bittensor ecosystem".', 'The codebase shows a clean Synapse-based architecture, Cloudflare R2 for clip hosting, and an external scoring server for quality metrics. Named founders are not individually listed at the time of writing — treat founder data as placeholder.'], founders: [{ initials: 'TH', gradient: 'v', name: '[TalkHead founders]', role: 'Founding team', bio: 'Authors of the talkheadai/talkhead-subnet codebase; public team page not yet published.' }], size: 'Small core team', founded: '2025', based: 'Distributed / remote', backers: 'Not publicly disclosed.', placeholder: true },
  milestones: [
    { date: '2025·Q4', text: 'Subnet 108 registered on Bittensor mainnet as TalkHead.' },
    { date: '2025·Q4', text: 'talkheadai/talkhead-subnet repo public with TalkHead Synapse, Cloudflare R2 upload, and external scoring server.' },
    { date: '2026·Q1', text: 'Subnet listed on subnetalpha.ai and bittensor.ai/subnets as a distributed evaluation platform for talking-head models.' },
  ],
  join: { title: 'Render the next clip', body: 'Stand up a talking-head model behind an HTTP API, plug it into a TalkHead miner that handles the Synapse + R2 upload, and let validators score your clips on sync + identity + motion + latency.', asideNote: 'Code: github.com/talkheadai/talkhead-subnet · live state: taostats.io/subnets/108/.' },
  tags: ['talking-head', 'video-generation', 'lip-sync', 'avatars', 'cloudflare-r2'],
  external: { github: 'https://github.com/talkheadai/talkhead-subnet', taostats: 'https://taostats.io/subnets/108/' },
};
